<% if @announcements.any? %>
  <ol class="post">
    <%= render @announcements %>
  </ol>
  <div class="center"><%= will_paginate @announcements, previous_label: "<", next_label: ">" %></div>
<% end %>
